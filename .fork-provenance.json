{
  "url": "https://github.com/agentclientprotocol/claude-agent-acp.git",
  "org": "agentclientprotocol/claude-agent-acp",
  "redirectFrom": "zed-industries/claude-code-acp",
  "tag": "v0.57.0",
  "sha": "2bf865eb42bbe744c476d38a005444eab8f4b624",
  "tagShaVerified": true,
  "versionRecords": {
    "note": "Each tree carries three version records that answer three different questions; they are not expected to be equal. This block is the normative statement of what each one governs. Prose home: docs/FORK.md. Machine-checked by test/version-records.test.ts.",
    "packageJsonVersion": "The npm identity of THIS tree. In the fork tree it is INERT - the fork is never published, and fork/.github/workflows/ is a documentary copy of the upstream identity that has never run. In the mirror tree it is the published version and is owned by release-please; never edit it by hand.",
    "releasePleaseManifest": "release-please's release state for THIS tree. In the fork tree it is an UPSTREAM-TRACKING record: it MUST equal forkProvenanceTag without the leading 'v', and CHANGELOG.md's newest entry MUST carry that same version. In the mirror tree it is LIVE and MUST equal packageJsonVersion.",
    "forkProvenanceTag": "The upstream tag currently vendored into src/ - the single live anchor for the fork point, re-anchored by sync stories (009 -> v0.39.0, 070 -> v0.53.0, 074 -> v0.57.0). It is independent of packageJsonVersion in both trees."
  },
  "upstreamDependencyKeys": ["@agentclientprotocol/sdk", "@anthropic-ai/claude-agent-sdk", "zod"],
  "externalRuntimeDeps": [
    {
      "name": "claude",
      "version": "2.1.159",
      "resolvedFrom": "PATH",
      "npm": false,
      "note": "The claude TUI binary is an EXTERNAL runtime dependency resolved from the user's PATH, NOT an npm dependency. The PTY engine (story 013) spawns it."
    }
  ],
  "openTodos": [
    {
      "tag": "🧪",
      "topic": "confirm-zed-version",
      "note": "Confirm the user's installed Zed version. The user's Zed protocolVersion and its tolerance of UNSTABLE fields (e.g. usage_update) are unverified (§15).",
      "deferredTo": "story 025"
    }
  ],
  "anchorRule": "anchor by symbol NAME, not line number — upstream line numbers are volatile (across v0.39→v0.53 createSession moved ≈L1933→L3098, canUseTool ≈L1600→L2621, prompt ≈L732→L1034)",
  "modules": ["index.ts", "acp-agent.ts", "tools.ts", "settings.ts", "utils.ts", "lib.ts"],
  "symbolInventory": {
    "acp-agent.ts": [
      "createSession",
      "prompt",
      "canUseTool",
      "toAcpNotifications",
      "streamEventToAcpNotifications",
      "replaySessionHistory",
      "runAcp",
      "initialize"
    ],
    "tools.ts": [
      "toolInfoFromToolUse",
      "toolUpdateFromToolResult",
      "toolUpdateFromDiffToolResponse",
      "planEntries",
      "taskStateToPlanEntries"
    ]
  }
}
